{
  "name": "trade-train",
  "private": true,
  "version": "2.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "type-check": "tsc --noEmit",
    "init-db": "node dist/scripts/init-db.js",
    "train": "node dist/scripts/train.js",
    "validate": "node dist/scripts/train.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.5",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
